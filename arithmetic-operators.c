#include <stdio.h>

int main()
{

    // arithmetic operators = +, -, *, /, %, ++, --

    int a = 10;
    int b = 5;
    int c = 0;

    // c = a + b;
    // c = a - b;
    // c = a * b;
    // Apabila integer dibagi dengan integer, hasilnya juga integer
    // Jika ingin hasilnya float, salah satu operand harus float atau double
    // c = a / b;
    // c = a % b;

    // Augmented assignment operators
    a += b; // a = a + b

    printf("%d", a);

    // printf("%d", c)

    return 0;
}